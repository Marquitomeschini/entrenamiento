# Gym App Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** PWA en GitHub Pages con la rutina del entrenador (videos de Drive, series/reps, registro de pesos, timer de descanso), alimentación y glosario.

**Architecture:** Sitio estático sin build: `index.html` + 5 archivos JS. Router por hash renderiza strings HTML en `#view`. Datos embebidos en `data.js`. Persistencia localStorage detrás de `Store` async. Checks con node.

**Tech Stack:** HTML/CSS/JS vanilla, Google Fonts (Barlow Condensed), node 22 solo para checks, `gh` CLI para deploy.

## Global Constraints

- Spec: `docs/superpowers/specs/2026-09-12-gym-app-design.md`. Fuentes en `docs/fuentes/*.pdf` (gitignored) y `drive_videos.json`.
- Cero dependencias npm. Cero frameworks. Cero build.
- Nunca meter texto de datos sin `esc()` en HTML.
- Tema único dark; `body` con `background` explícito.
- Tap targets ≥ 44 px. `padding-bottom: env(safe-area-inset-bottom)` en la nav.
- Commits pequeños al final de cada tarea, mensaje en español, con `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.

---

### Task 1: Datos + check

**Files:**
- Create: `data.js`, `check_data.js`

**Interfaces:**
- Produces: `window.DATA = { plan, videos, comida, tecnicas }` con el shape exacto de abajo. Todo lo demás lo consume.

- [ ] **Step 1: Escribir `check_data.js`**

```js
const fs = require('fs'), vm = require('vm');
const ctx = { window: {} };
vm.runInNewContext(fs.readFileSync(__dirname + '/data.js', 'utf8'), ctx);
const { plan, videos, comida, tecnicas } = ctx.window.DATA;
const ids = new Set(JSON.parse(fs.readFileSync(__dirname + '/drive_videos.json', 'utf8')).flatMap(f => (f.children || []).map(c => c.id)));
const errs = [];
if (plan.dias.length !== 4) errs.push('plan: se esperan 4 días');
for (const d of plan.dias) {
  if (!d.bloques?.length) errs.push(`${d.id}: sin bloques`);
  for (const b of d.bloques || []) {
    const n = b.ejercicios?.[0]?.nombre;
    if (!(b.series > 0)) errs.push(`${d.id}: series inválidas (${n})`);
    if (typeof b.descanso !== 'number') errs.push(`${d.id}: descanso no numérico (${n})`);
    if (!Array.isArray(b.badges)) errs.push(`${d.id}: badges no es array (${n})`);
    if (!b.ejercicios?.length) errs.push(`${d.id}: bloque sin ejercicios`);
    for (const e of b.ejercicios || []) if (e.video && !ids.has(e.video)) errs.push(`${d.id}: video inexistente: ${e.nombre} → ${e.video}`);
  }
  if (d.cardio?.video && !ids.has(d.cardio.video)) errs.push(`${d.id}: video cardio inexistente`);
}
if (videos.length !== ids.size) errs.push(`videos: ${videos.length} en data.js vs ${ids.size} en Drive`);
for (const v of videos) if (!ids.has(v.id)) errs.push(`videos: id inexistente ${v.titulo}`);
for (const m of comida.comidas) if (!m.opciones?.length) errs.push(`comida: ${m.nombre} sin opciones`);
if (!tecnicas.length) errs.push('tecnicas vacío');
if (errs.length) { console.error(errs.join('\n')); process.exit(1); }
console.log(`OK · ${plan.dias.length} días · ${videos.length} videos · ${comida.comidas.length} comidas · ${tecnicas.length} técnicas`);
```

- [ ] **Step 2: Correr y ver que falla**

Run: `node check_data.js`
Expected: error `Cannot find module` / `data.js` no existe → exit 1.

- [ ] **Step 3: Escribir `data.js`**

Transcribir desde `docs/fuentes/Plan_MarcoMeschini.pdf` y `Alimentacion_MarcoMeschini.pdf`. Shape y Día 1 completo como referencia; Días 2-4, comida y técnicas siguen idéntico shape. Descanso en segundos (`1'30"` → 90, `2'` → 120, `2'30"` → 150, `—` → 0). `aprox` solo cuando hay series de aproximación. Nombres en Title case normal (no mayúsculas).

```js
window.DATA = {
  plan: { mes: 1, dias: [
    { id: 'lun', dia: 1, titulo: 'Torso completo', bloques: [
      { aprox: 2, series: 3, reps: '10 - 8 - 6 subiendo el peso', descanso: 90, badges: ['APROX'],
        ejercicios: [{ nombre: 'Pect deck', nota: '2 series de aproximación (5 y 3 reps) con peso ligero + 3 efectivas.', video: '1sYMlhKOOZZdZc4qABt5ywxXvQhYpepGj' }] },
      { series: 3, reps: '20 - 15 - 12 · dropset y fallo en la última', descanso: 90, badges: ['DROPSET', 'FALLO'],
        ejercicios: [{ nombre: 'Pullover con polea soga', nota: 'Mantener 2 seg arriba y 2 seg abajo.', video: '1YBeCt9UwF80RYhNFNNsytD44LB6BjhXH' }] },
      { aprox: 2, series: 2, reps: '10 - 6 · dropset y fallo en la última', descanso: 120, badges: ['APROX', 'DROPSET', 'FALLO'],
        ejercicios: [{ nombre: 'Press inclinado con mancuernas', nota: '2 series de aproximación (3 y 2 reps) con peso ligero + 2 efectivas.', video: '1Inz38000Wa_oKI9ovOB2X5SobloBPMT4' }] },
      { series: 2, reps: '10 - 6', descanso: 90, badges: [],
        ejercicios: [{ nombre: 'Jalón en dorsalera agarre unilateral neutro', nota: 'Inclinarse hacia el lado contrayendo los oblicuos.', video: '1oR58_IRjLuhFJA9Y-C3Mm-4K3bBwiIK1' }] },
      { series: 3, reps: '10 - 8 - 6 subiendo el peso', descanso: 90, badges: [],
        ejercicios: [{ nombre: 'Press de hombros con mancuernas sentado', video: '1lFEraIA3AcWH9zHcrOq1A_JoTnjOUWLv' }] },
      { series: 3, reps: '15 - 10 - 8 en ambos, subiendo el peso', descanso: 60, badges: ['BISERIE'],
        ejercicios: [{ nombre: 'Press plano en máquina con placas' }, { nombre: 'Curl de bíceps martillo en simultáneo con mancuernas', video: '1mUnTrondsdWRE0WXGyupH9yacUbqMjuK' }] },
      { series: 3, reps: '12 - 8 - 6 en ambos · dropset y fallo en la última de remo', descanso: 60, badges: ['BISERIE', 'DROPSET', 'FALLO'],
        ejercicios: [{ nombre: 'Remo Gironda en máquina', video: '12byDyN3ASYpx2dyWCTNOYu3Dxne9-ZZx' }, { nombre: 'Extensión de tríceps trasnuca con mancuerna a 2 manos' }] },
      { series: 2, reps: '15 - 10 en ambos · dropset y fallo x2 en la última de curl', descanso: 60, badges: ['BISERIE', 'DROPSET', 'FALLO'],
        ejercicios: [{ nombre: 'Extensión de tríceps unilateral en polea con manopla', video: '1K_QU1YffvcuzwmhrrsWS5dbjBrPSJIFA' }, { nombre: 'Curl de bíceps con polea barra', video: '1FuIr0YTlL0mRv9NrkZAZnIYtCBu7TaMp' }] },
      { series: 3, reps: '8 - fallo - fallo · triple dropset', descanso: 60, badges: ['DROPSET', 'FALLO'],
        ejercicios: [{ nombre: 'Vuelos laterales con mancuernas sentado', nota: 'Triple dropset. 3 series de 8 reps y fallo.', video: '19bX9TLAWEd6avohJQVJyLLNqlSiRp81p' }] },
      { series: 3, reps: '20 - 15 - 12 en ambos, subiendo el peso', descanso: 60, badges: ['BISERIE'],
        ejercicios: [{ nombre: 'Vuelos posteriores en pect deck', video: '1E809RsvQoE6gSJDHEdwrihya_aBQs5ne' }, { nombre: 'Curl de bíceps supino en simultáneo sentado en banco, codos rígidos', video: '1iUwLfPv09ZQb6fV0lX_EasJ4m-fk20Nz' }] },
    ], cardio: { titulo: 'Cardio HIIT — post entrenamiento', sub: '3 vueltas · 1 min 30 seg de descanso entre vueltas', pasos: [
      { nombre: 'Burpees', nota: 'Plancha completa, subís y saltás palmeando arriba.', dosis: '8 reps' },
      { nombre: 'Angelitos / jumping jacks', nota: 'Brazos y piernas coordinados. Ritmo máximo.', dosis: '40 seg' },
      { nombre: 'Sentadillas con salto', nota: 'Rodillas al pecho. Aterrizás suave y semiflexionado.', dosis: '20 reps' },
      { nombre: 'Mountain climbers', nota: 'Desde plancha alta. Rodillas al pecho alternando.', dosis: '30 seg' },
    ] } },
    // mar (dia: 2, 'Piernas completo'), mie (dia: 3, 'Torso'), vie (dia: 5, 'Torso + intensidad'): transcribir del PDF págs. 4-10
  ] },
  videos: [
    { grupo: 'Abdominales', titulo: 'Crunch abdominal con piernas elevadas', id: '12SzEuicNeXrrQdkijze-KNy8HSWyTt3g' },
    // ... los 47 de drive_videos.json, título limpio (sin .mp4/.mov, sin typos: "ABODMINAL" → "abdominal", "EXTENSIÓNE" → "Extensión de", "FRANCÉSCON" → "francés con")
  ],
  comida: { kcal: 2200, macros: { P: 115, G: 60, C: 250 }, comidas: [
    { nombre: 'Desayuno', kcal: '~730 kcal · 4 opciones', opciones: [
      { nombre: 'Opción 1', kcal: 692, P: 34, G: 22, C: 98, items: ['2 claras de huevo + 2 huevos enteros', '82 g avena (peso crudo)', '1 banana (128 g)', '13 g mantequilla de maní'] },
      // ... opciones 2-4
    ] },
    { nombre: 'Almuerzo', kcal: '~750 kcal · 5 opciones', opciones: [ /* PDF págs. 3-4 */ ], nota: 'Intercambios proteína: 170 g pollo crudo = 190 g milanesa de pollo · 170 g carne cruda = 210 g milanesa.' },
    { nombre: 'Cena', kcal: '~750 kcal · 5 opciones', opciones: [ /* PDF pág. 5 */ ] },
  ], reglas: [
    { titulo: 'Pesos crudos', texto: 'Todos los pesos son antes de cocinar. Arroz: 100 g crudo rinde ~280 g cocido. Pollo: 170 g crudo → ~128 g cocido. Merluza: 170 g crudo → ~136 g cocida.' },
    { titulo: 'Aceite', texto: 'Máximo 1 cucharada (12 g) por comida principal. No más de 2 cucharadas por día en total.' },
    { titulo: 'Verduras', texto: 'Libres en almuerzo y cena. Sin límite de cantidad.' },
    { titulo: 'Agua', texto: 'Mínimo 2,5 a 3 litros por día.' },
    { titulo: 'Cocción', texto: 'Horno, plancha, air fryer o vapor. Sin fritura.' },
    { titulo: 'Equivalencias', texto: '128 g pollo cocido = 143 g milanesa de pollo · 128 g carne cocida = 158 g milanesa.' },
  ] },
  tecnicas: [
    { nombre: 'BISERIE', texto: 'Dos ejercicios seguidos sin descanso. El descanso indicado va al terminar los dos.' },
    { nombre: 'SUPERSERIE', texto: 'Dos ejercicios de músculos opuestos seguidos sin descanso.' },
    { nombre: 'SERIE GIGANTE', texto: 'Tres o más ejercicios seguidos sin descanso con rest pause entre rondas.' },
    { nombre: 'DROPSET', texto: 'Llegás al fallo, bajás el peso 20-30% de inmediato y seguís sin descanso.' },
    { nombre: 'FALLO', texto: 'No podés completar ni una rep más con buena técnica. Sin trampa.' },
    { nombre: 'APROX', texto: 'Series de calentamiento con peso liviano. No cuentan como trabajo real.' },
  ],
};
```

Mapeo de videos para Días 2-4 (id de Drive; `—` = sin video):

| Ejercicio (plan) | Video |
|---|---|
| Extensión de cuádriceps | `18vYJAYCpYwRl8sUU-8PYzKGQmi-2LMD9` |
| Sentadilla en Smith | `1WQr-FBtumZg8QLTtn8xT5PNp6yXN-QU8` |
| Prensa pies anchos | `1ULzO9uPU7EtOR0L9WY7-lN-Ri28qPgc1` |
| Estocadas caminando | — |
| Curl femoral unilateral parado o acostado | `1YZ9t3shIZrHE1evZAtlVPJ9nCelbjT6m` |
| Peso muerto rumano con mancuernas | — |
| Abductor en máquina | — |
| Aductor | `1_X4IP_s7HObZ3BM_x9RgxaVxGBllVsCS` |
| Elevación de gemelos en prensa | — |
| Aperturas con mancuernas inclinadas | — |
| Vuelos posteriores en pect deck | `1E809RsvQoE6gSJDHEdwrihya_aBQs5ne` |
| Press inclinado en máquina Hammer | — |
| Jalón en dorsalera agarre supino con lastre | — |
| Remo al mentón con barra Z | — |
| Vuelos laterales con mancuernas parado | `19bX9TLAWEd6avohJQVJyLLNqlSiRp81p` |
| Jalón prono en dorsalera | `1TXkbZEJBwWekdCiVwZLcj1Xb0434HkLS` |
| Curl de bíceps supino con barra Z | — |
| Press francés con barra Z | — |
| Curl de bíceps martillo con polea unilateral | `1H2lcAYguaJuwVbkq9t8tGGF6PPLSs_RJ` |
| Extensión de tríceps con polea soga | `1Rs5aHfVbXC5eq1DM4d5RBEYbN2OqiJZG` |
| Vuelos laterales alternos con mancuernas | `1m4QCfp_nzwroJp5KETYAO8_3I3-aNRge` |
| Cruces con polea baja (serie gigante) | `1dmFo_AKoBk9LPL-tmN29PyExRwJosQwd` |
| Pasadas en cinta (cardio mié) | `cardio.video = '1cR38DQeLNh0h7X0WGgueOL0AABqqGWp2'` |
| Press de hombros militar en Smith | — |
| Press inclinado en Smith | — |
| Remo con barra agarre prono | — |
| Jalón en dorsalera agarre neutro | `1aJuJQ0y9XVlgnPlIWxCrsfmWQNWeMMpK` |
| Jalón en dorsalera agarre prono | `1TXkbZEJBwWekdCiVwZLcj1Xb0434HkLS` |
| Press plano con mancuernas | `1nAOrcg2VXFdfQ_plhsC7PDUS_kosp_VR` |
| Curl de bíceps sentado supino con mancuernas | `1iUwLfPv09ZQb6fV0lX_EasJ4m-fk20Nz` |
| Remo en polea agarre supino con barra | — |
| Fondos para tríceps con peso y pies sobre banco | — |
| Extensión de tríceps con polea a 1 mano manopla | `1K_QU1YffvcuzwmhrrsWS5dbjBrPSJIFA` |
| Curl de bíceps en polea barra agarre prono | — |
| Curl de bíceps en banco inclinado supino | `1LJ15BBVzDlk4gMozdUR6UwHM9JixFz7W` |
| Press francés con mancuernas en banco inclinado | `1aWfwf-hv0t-h6NW9xkb-3RR9-m2w1W0Q` |
| Vuelos laterales a una mano con polea tobillera | `13OHlzX9n5FKteNVSbf8-4u4JIwWiEH-o` |
| Face pull con polea soga | `1Qlrzk2tdOYiYdVamqR04zC6TQexcbJ3L` |
| Extensión trasnuca con polea soga | `1bsgBx3qN-tfK_VnNDwXMhn5q8XMV__ZU` |
| Curl martillo con polea soga | `1H2lcAYguaJuwVbkq9t8tGGF6PPLSs_RJ` |
| Finisher — flexiones diamantes | — |

Cardio Día 2 (`Caminata en cinta con inclinación 10`, sub `Post piernas · 20 minutos totales`): 4 pasos de 5 min (5 km/h · 6,5 · 5 · 7, todos inclinación 10%). Cardio Día 3 (`Pasadas en cinta`, sub `30 seg de descanso entre cada pasada`): 7 pasos (trote 7 km/h 3 min; trote rápido 8 km/h 1:30; sprint ×2 a 10 km/h 40 seg c/u; ×2 a 11, 12, 13 km/h 30 seg c/u; sprint final 14 km/h 30 seg). Cardio Día 4 = igual al Día 1 (HIIT).

- [ ] **Step 4: Correr el check hasta que pase**

Run: `node check_data.js`
Expected: `OK · 4 días · 47 videos · 3 comidas · 6 técnicas`

- [ ] **Step 5: Commit**

```bash
git add data.js check_data.js && git commit -m "Datos: plan mes 1, 47 videos, alimentación, técnicas + check"
```

---

### Task 2: Shell, estilos, router, Rutina y Día

**Files:**
- Create: `index.html`, `util.js`, `app.js`, `test.js`, `.claude/launch.json`

**Interfaces:**
- Consumes: `window.DATA` (Task 1).
- Produces: `util.js` exporta `{ slug, fmtDesc, diaDeHoy, hoyISO, ultimaVez, esc }` (globales en browser vía `const`, `module.exports` en node). `app.js` define `routes` (objeto `nombre → fn(...args) → string | Promise<string>`), `view` (`#view`), `badge(b)`, `video(id)`. Task 3 y 5 agregan entradas a `routes`.

- [ ] **Step 1: Escribir `test.js`**

```js
const assert = require('assert');
const { slug, fmtDesc, diaDeHoy, hoyISO, ultimaVez, esc } = require('./util.js');
assert.equal(slug('Jalón en dorsalera agarre neutro'), 'jalon-en-dorsalera-agarre-neutro');
assert.equal(fmtDesc(90), `1'30"`);
assert.equal(fmtDesc(60), `1'`);
assert.equal(fmtDesc(150), `2'30"`);
assert.equal(fmtDesc(0), '—');
const dias = [{ id: 'lun', dia: 1 }, { id: 'vie', dia: 5 }];
assert.equal(diaDeHoy(dias, new Date(2026, 8, 14)).id, 'lun'); // lunes
assert.equal(diaDeHoy(dias, new Date(2026, 8, 13)), null);      // domingo
assert.equal(hoyISO(new Date(2026, 8, 12, 23, 30)), '2026-09-12'); // local, no UTC
const log = { sesiones: [
  { fecha: '2026-09-07', series: [{ kg: 20, reps: 10 }] },
  { fecha: '2026-09-10', series: [null, { kg: 22, reps: 8 }] },
  { fecha: '2026-09-11', series: [{ reps: 8 }] },
  { fecha: '2026-09-12', series: [{ kg: 24, reps: 6 }] },
] };
assert.equal(ultimaVez(log, '2026-09-12').fecha, '2026-09-10');
assert.equal(ultimaVez({ sesiones: [] }, '2026-09-12'), null);
assert.equal(ultimaVez(null, '2026-09-12'), null);
assert.equal(esc('<b>&"'), '&lt;b&gt;&amp;&quot;');
console.log('OK util');
```

- [ ] **Step 2: Correr y ver que falla**

Run: `node test.js`
Expected: `Cannot find module './util.js'`

- [ ] **Step 3: Escribir `util.js`**

```js
const DIAS = ['Dom', 'Lun', 'Mar', 'Mié', 'Jue', 'Vie', 'Sáb'];
const slug = s => s.toLowerCase().normalize('NFD').replace(/[̀-ͯ]/g, '').replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
const fmtDesc = s => !s ? '—' : s % 60 ? `${Math.floor(s / 60)}'${String(s % 60).padStart(2, '0')}"` : `${s / 60}'`;
const diaDeHoy = (dias, d = new Date()) => dias.find(x => x.dia === d.getDay()) || null;
const hoyISO = (d = new Date()) => `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`;
const ultimaVez = (log, hoy) => {
  const s = (log?.sesiones || []).filter(x => x.fecha !== hoy && (x.series || []).some(y => y?.kg));
  return s.length ? s[s.length - 1] : null;
};
const esc = s => String(s ?? '').replace(/[&<>"]/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;' }[c]));
if (typeof module !== 'undefined') module.exports = { DIAS, slug, fmtDesc, diaDeHoy, hoyISO, ultimaVez, esc };
```

- [ ] **Step 4: Correr el test**

Run: `node test.js`
Expected: `OK util`

- [ ] **Step 5: Escribir `index.html`**

```html
<!doctype html>
<html lang="es">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
<title>Entrenamiento</title>
<meta name="theme-color" content="#0c0b09">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="Gym">
<link rel="manifest" href="manifest.json">
<link rel="apple-touch-icon" href="apple-touch-icon.png">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@600;800&display=swap">
<style>
:root{--bg:#0c0b09;--sur:#161410;--sur2:#1e1b15;--line:#2a261d;--tx:#f2eee6;--mut:#a39c8c;--gold:#c9a84c;
--aprox:#1fb6c9;--biserie:#c9a84c;--superserie:#3b82f6;--serie-gigante:#e6d28a;--dropset:#d33b3b;--fallo:#e58a1f;
--desayuno:#c9a84c;--almuerzo:#3b82f6;--cena:#b06cf5;--disp:"Barlow Condensed",Impact,"Arial Narrow",sans-serif}
*{box-sizing:border-box}html{-webkit-text-size-adjust:100%}
body{margin:0;background:var(--bg);color:var(--tx);font:16px/1.45 -apple-system,system-ui,sans-serif;padding-bottom:calc(72px + env(safe-area-inset-bottom))}
a{color:inherit;text-decoration:none}
main{max-width:560px;margin:0 auto;padding:0 16px}
h1,h2{font-family:var(--disp);text-transform:uppercase;letter-spacing:.02em;margin:0;text-wrap:balance}
h1{font-size:40px;line-height:1;font-weight:800}h2{font-size:22px;font-weight:700;color:var(--gold);margin:28px 0 8px}
.eyebrow{display:block;font-size:12px;letter-spacing:.12em;text-transform:uppercase;color:var(--mut)}
.top{padding:calc(16px + env(safe-area-inset-top)) 0 12px}.back{display:inline-block;color:var(--gold);font-weight:600;margin-bottom:10px;min-height:44px;line-height:44px}
.hero{display:block;background:var(--sur);border:1px solid var(--line);border-left:4px solid var(--gold);padding:20px;margin:8px 0 20px}
.hero h1{margin:4px 0 12px}.hero .cta{color:var(--gold);font-weight:600}.hero.rest{border-left-color:var(--line)}.hero p{margin:0;color:var(--mut)}
.list{display:flex;flex-direction:column;border-top:1px solid var(--line)}
.row{display:flex;justify-content:space-between;gap:12px;padding:14px 0;border-bottom:1px solid var(--line);min-height:56px;align-items:center}
.row.multi{border-left:3px solid var(--gold);padding-left:12px}
.row .names{display:flex;flex-direction:column;gap:4px;min-width:0}.row b{font-weight:600}.row .con{font-size:12px;color:var(--gold);font-weight:700}
.row .nums{display:flex;flex-direction:column;align-items:flex-end;text-align:right;flex-shrink:0;font-variant-numeric:tabular-nums;max-width:45%}
.row .ser{font-family:var(--disp);font-size:26px;font-weight:800;line-height:1}.row .reps,.row .desc{font-size:12px;color:var(--mut)}.row .desc{color:var(--gold)}
.row .play{color:var(--gold);font-size:14px}
.badges{display:flex;flex-wrap:wrap;gap:6px;margin-top:2px}
.b{font:700 10px/1 system-ui;letter-spacing:.08em;padding:5px 7px;border-radius:3px;color:#0c0b09;background:var(--mut);text-transform:uppercase;font-style:normal}
.b-aprox{background:var(--aprox)}.b-biserie{background:var(--biserie)}.b-superserie{background:var(--superserie);color:#fff}.b-serie-gigante{background:var(--serie-gigante)}.b-dropset{background:var(--dropset);color:#fff}.b-fallo{background:var(--fallo)}
.cardio{background:var(--sur);border:1px solid var(--line);padding:16px;margin-top:28px}.cardio h2{margin:0 0 2px}.cardio .sub{margin:0 0 12px;color:var(--mut);font-size:13px}
.cardio ol{list-style:none;margin:0;padding:0;counter-reset:p}.cardio li{display:grid;grid-template-columns:28px 1fr auto;gap:2px 10px;padding:10px 0;border-top:1px solid var(--line);counter-increment:p}
.cardio li::before{content:counter(p);font-family:var(--disp);font-weight:800;color:var(--gold);font-size:20px;grid-row:span 2}.cardio li b{font-weight:600}.cardio li small{grid-column:2;color:var(--mut);font-size:13px}.cardio .dosis{color:var(--gold);font-weight:700;font-variant-numeric:tabular-nums;white-space:nowrap}
.spec{display:flex;gap:16px;flex-wrap:wrap;margin:6px 0 8px;color:var(--mut);font-size:14px}.spec b{color:var(--tx);font-family:var(--disp);font-size:22px;font-weight:800}
.ej{margin-top:20px}.ej h2{color:var(--tx);margin:12px 0 4px}.nota{margin:0 0 10px;color:var(--mut);font-size:14px}.ult{margin:0 0 8px;color:var(--gold);font-size:14px;font-variant-numeric:tabular-nums}
.vid{aspect-ratio:3/4;max-height:62vh;background:var(--sur);border:1px solid var(--line)}.vid iframe{width:100%;height:100%;border:0;display:block}
.novideo{padding:18px;background:var(--sur);border:1px dashed var(--line);color:var(--mut);text-align:center;font-size:14px}
.log{width:100%;border-collapse:collapse;font-variant-numeric:tabular-nums}.log th{font-size:11px;letter-spacing:.1em;text-transform:uppercase;color:var(--mut);font-weight:600;text-align:left;padding:4px 6px}
.log td{padding:4px 6px;border-top:1px solid var(--line)}.log td:first-child{width:52px;color:var(--gold);font-family:var(--disp);font-size:20px;font-weight:800}
.log input{width:100%;min-height:44px;background:var(--sur2);border:1px solid var(--line);color:var(--tx);font:18px system-ui;padding:6px 10px;border-radius:4px}.log input:focus{outline:2px solid var(--gold);outline-offset:-1px}
.actions{display:flex;flex-direction:column;gap:10px;margin:24px 0 8px}
.btn{display:block;min-height:52px;line-height:52px;text-align:center;background:var(--gold);color:#0c0b09;font:700 17px system-ui;border:0;border-radius:6px;cursor:pointer;width:100%}
.btn.ghost{background:transparent;color:var(--gold);border:1px solid var(--gold)}.btn:focus-visible{outline:3px solid #fff}
.macros{display:grid;grid-template-columns:repeat(3,1fr);gap:1px;background:var(--line);border:1px solid var(--line);margin:12px 0}.macros div{background:var(--sur);padding:12px;text-align:center}.macros small{display:block;font-size:11px;letter-spacing:.1em;text-transform:uppercase;color:var(--mut)}.macros b{font-family:var(--disp);font-size:26px;font-weight:800}
.meal h2 small{font-family:system-ui;text-transform:none;letter-spacing:0;font-size:13px;color:var(--mut);margin-left:8px;font-weight:400}
.m-desayuno h2{color:var(--desayuno)}.m-almuerzo h2{color:var(--almuerzo)}.m-cena h2{color:var(--cena)}
details{border:1px solid var(--line);background:var(--sur);margin-bottom:8px}summary{display:flex;flex-wrap:wrap;align-items:baseline;gap:8px;padding:12px 14px;cursor:pointer;min-height:44px;list-style:none}summary::-webkit-details-marker{display:none}
summary b{font-weight:600}summary span{color:var(--gold);font-variant-numeric:tabular-nums}summary small{width:100%;color:var(--mut);font-size:12px}
details ul{margin:0;padding:0 14px 12px 30px;color:var(--tx)}details li{padding:3px 0}
.reglas{margin:0}.reglas dt{margin-top:14px;font-weight:700;color:var(--gold);text-transform:uppercase;font-size:12px;letter-spacing:.1em}.reglas dd{margin:4px 0 0;color:var(--tx)}
nav{position:fixed;left:0;right:0;bottom:0;display:flex;background:var(--sur);border-top:1px solid var(--line);padding-bottom:env(safe-area-inset-bottom)}
nav a{flex:1;text-align:center;padding:10px 0 8px;min-height:56px;font-size:11px;letter-spacing:.06em;text-transform:uppercase;color:var(--mut);font-weight:600}
nav a .ic{display:block;font-family:var(--disp);font-size:22px;line-height:1;margin-bottom:4px;font-weight:800}nav a.on{color:var(--gold)}
#timer{position:fixed;inset:0;background:var(--bg);display:flex;flex-direction:column;align-items:center;justify-content:center;gap:24px;z-index:9}
#timer b{font-family:var(--disp);font-size:120px;line-height:1;font-weight:800;font-variant-numeric:tabular-nums}#timer.done{background:var(--dropset)}#timer.done b{color:#fff}
#timer .btn{width:min(320px,80vw)}#timer small{color:var(--mut);letter-spacing:.12em;text-transform:uppercase;font-size:12px}
@media(prefers-reduced-motion:no-preference){#timer.done{animation:flash .6s 3}@keyframes flash{50%{background:var(--bg)}}}
</style>
</head>
<body>
<main id="view"></main>
<div id="timer" hidden><small>Descanso</small><b>0:00</b><button class="btn" id="timer-close">Listo</button></div>
<nav>
  <a href="#/rutina" data-tab="rutina"><span class="ic">R</span>Rutina</a>
  <a href="#/videos" data-tab="videos"><span class="ic">▶</span>Videos</a>
  <a href="#/comida" data-tab="comida"><span class="ic">K</span>Comida</a>
  <a href="#/tecnicas" data-tab="tecnicas"><span class="ic">?</span>Técnicas</a>
</nav>
<script src="data.js"></script>
<script src="util.js"></script>
<script src="store.js"></script>
<script src="timer.js"></script>
<script src="app.js"></script>
</body>
</html>
```

(`store.js` y `timer.js` se crean en Tasks 3 y 4; hasta entonces el 404 no rompe nada.)

- [ ] **Step 6: Escribir `app.js` con router + Rutina + Día**

```js
const view = document.getElementById('view');
const badge = b => `<i class="b b-${slug(b)}">${esc(b)}</i>`;
const video = id => `<div class="vid"><iframe src="https://drive.google.com/file/d/${encodeURIComponent(id)}/preview" allow="autoplay; fullscreen" loading="lazy" title="Video del ejercicio"></iframe></div>`;
const serTxt = b => b.aprox ? `${b.aprox}+${b.series}` : String(b.series);
const dia = id => DATA.plan.dias.find(x => x.id === id);

function renderRutina() {
  const hoy = diaDeHoy(DATA.plan.dias), d = new Date();
  const prox = DATA.plan.dias.find(x => x.dia > d.getDay()) || DATA.plan.dias[0];
  const hero = hoy
    ? `<a class="hero" href="#/dia/${hoy.id}"><span class="eyebrow">Hoy · ${DIAS[hoy.dia]}</span><h1>${esc(hoy.titulo)}</h1><span class="cta">Empezar ›</span></a>`
    : `<div class="hero rest"><span class="eyebrow">Hoy · ${DIAS[d.getDay()]}</span><h1>Descanso</h1><p>Próximo: ${DIAS[prox.dia]} · ${esc(prox.titulo)}</p></div>`;
  const rows = DATA.plan.dias.map(x => `<a class="row" href="#/dia/${x.id}"><div class="names"><b>${DIAS[x.dia]} · ${esc(x.titulo)}</b><span class="eyebrow">${x.bloques.length} bloques${x.cardio ? ' + cardio' : ''}</span></div><span class="play">›</span></a>`).join('');
  return `<header class="top"><span class="eyebrow">Mes ${DATA.plan.mes} · 4 días por semana · @aguspazfit</span></header>${hero}<h2>Semana</h2><div class="list">${rows}</div>`;
}

function renderDia(id) {
  const d = dia(id); if (!d) return renderRutina();
  const rows = d.bloques.map((b, i) => `<a class="row${b.ejercicios.length > 1 ? ' multi' : ''}" href="#/ej/${d.id}/${i}">
    <div class="names">${b.ejercicios.map(e => `<b>${esc(e.nombre)}</b>`).join('<span class="con">+</span>')}${b.badges.length ? `<div class="badges">${b.badges.map(badge).join('')}</div>` : ''}</div>
    <div class="nums"><span class="ser">${serTxt(b)}</span><span class="reps">${esc(b.reps)}</span><span class="desc">${fmtDesc(b.descanso)}</span></div></a>`).join('');
  const c = d.cardio;
  const cardio = c ? `<section class="cardio"><h2>${esc(c.titulo)}</h2><p class="sub">${esc(c.sub)}</p><ol>${c.pasos.map(p => `<li><b>${esc(p.nombre)}</b><span class="dosis">${esc(p.dosis)}</span>${p.nota ? `<small>${esc(p.nota)}</small>` : ''}</li>`).join('')}</ol>${c.video ? `<a class="btn ghost" href="#/video/${c.video}" style="margin-top:12px">Ver técnica de running</a>` : ''}</section>` : '';
  return `<header class="top"><a class="back" href="#/rutina">‹ Rutina</a><span class="eyebrow">${DIAS[d.dia]}</span><h1>${esc(d.titulo)}</h1></header><div class="list">${rows}</div>${cardio}`;
}

const routes = { rutina: renderRutina, dia: renderDia };
const TAB = { dia: 'rutina', ej: 'rutina', video: 'videos' };

async function route() {
  const [name = 'rutina', ...args] = location.hash.replace(/^#\/?/, '').split('/').map(decodeURIComponent);
  const fn = routes[name] || renderRutina;
  view.innerHTML = await fn(...args);
  window.scrollTo(0, 0);
  const tab = TAB[name] || name;
  document.querySelectorAll('nav a').forEach(a => a.classList.toggle('on', a.dataset.tab === tab));
}
window.addEventListener('hashchange', route);
route();
```

- [ ] **Step 7: Crear `.claude/launch.json` y ver en el browser**

```json
{ "version": "0.0.1", "configurations": [ { "name": "gym", "runtimeExecutable": "python3", "runtimeArgs": ["-m", "http.server", "8765"], "port": 8765 } ] }
```

Abrir preview `gym` con viewport mobile (375×812). Verificar: Rutina muestra hero (hoy sábado → "Descanso · Próximo: Lun"), 4 filas; tap "Lun" → lista de 10 bloques con badges, biseries con rail dorado, cardio HIIT abajo; nav marca "Rutina"; sin scroll horizontal; consola sin errores salvo 404 de `store.js`/`timer.js`.

- [ ] **Step 8: Commit**

```bash
git add index.html util.js app.js test.js .claude/launch.json && git commit -m "Shell, router, pantallas Rutina y Día"
```

---

### Task 3: Store + pantalla Ejercicio con registro de pesos

**Files:**
- Create: `store.js`
- Modify: `app.js` (agregar `renderEjercicio`, ruta `ej`, listener `change`)

**Interfaces:**
- Consumes: `ultimaVez`, `hoyISO`, `slug` de `util.js`; `routes`, `view`, `badge`, `video`, `serTxt`, `dia` de `app.js`.
- Produces: `Store.get(k): Promise<obj|null>`, `Store.set(k, obj)`. Inputs con `data-k` (slug), `data-n` (índice serie), `data-f` (`kg`|`reps`). Botón `[data-timer="<seg>"]` que Task 4 engancha.

- [ ] **Step 1: Escribir `store.js`**

```js
// ponytail: solo localStorage — se pierde al borrar datos de Safari, sin sync. Upgrade: backend detrás de esta misma API async.
const Store = {
  get: k => { try { return Promise.resolve(JSON.parse(localStorage.getItem('gym:' + k))); } catch { return Promise.resolve(null); } },
  set: (k, v) => { try { localStorage.setItem('gym:' + k, JSON.stringify(v)); } catch {} },
};
```

- [ ] **Step 2: Agregar a `app.js` (antes de `const routes`)**

```js
async function renderEjercicio(id, i) {
  const d = dia(id), b = d?.bloques[+i]; if (!b) return renderRutina();
  const hoy = hoyISO();
  const parts = await Promise.all(b.ejercicios.map(async e => {
    const k = slug(e.nombre), log = await Store.get(k), ult = ultimaVez(log, hoy);
    const sh = log?.sesiones?.find(s => s.fecha === hoy);
    const filas = Array.from({ length: b.series }, (_, n) => {
      const s = sh?.series?.[n] || {}, u = ult?.series?.[n] || {};
      const inp = (f, ph, mode, step) => `<input type="number" inputmode="${mode}"${step ? ` step="${step}"` : ''} placeholder="${esc(u[f] ?? ph)}" value="${esc(s[f] ?? '')}" data-k="${k}" data-n="${n}" data-f="${f}" aria-label="${f} serie ${n + 1}">`;
      return `<tr><td>${n + 1}</td><td>${inp('kg', 'kg', 'decimal', '0.5')}</td><td>${inp('reps', 'reps', 'numeric')}</td></tr>`;
    }).join('');
    const ultTxt = ult ? `Última vez (${ult.fecha.slice(5).split('-').reverse().join('/')}): ${ult.series.filter(s => s?.kg).map(s => `${s.kg}×${s.reps ?? '?'}`).join(' · ')}` : 'Sin registro previo';
    return `<article class="ej">${e.video ? video(e.video) : '<div class="novideo">Sin video del entrenador para este ejercicio</div>'}
      <h2>${esc(e.nombre)}</h2>${e.nota ? `<p class="nota">${esc(e.nota)}</p>` : ''}<p class="ult">${ultTxt}</p>
      <table class="log"><thead><tr><th>Serie</th><th>kg</th><th>reps</th></tr></thead><tbody>${filas}</tbody></table></article>`;
  }));
  const next = d.bloques[+i + 1] ? `<a class="btn ghost" href="#/ej/${d.id}/${+i + 1}">Siguiente ›</a>` : `<a class="btn ghost" href="#/dia/${d.id}">Fin · volver al día</a>`;
  return `<header class="top"><a class="back" href="#/dia/${d.id}">‹ ${esc(d.titulo)}</a>
    <div class="spec"><span><b>${serTxt(b)}</b> series</span><span>${esc(b.reps)}</span><span>desc. <b>${fmtDesc(b.descanso)}</b></span></div>
    ${b.badges.length ? `<div class="badges">${b.badges.map(badge).join('')}</div>` : ''}</header>${parts.join('')}
    <div class="actions">${b.descanso ? `<button class="btn" data-timer="${b.descanso}">Descanso ${fmtDesc(b.descanso)}</button>` : ''}${next}</div>`;
}

view.addEventListener('change', async ev => {
  const el = ev.target; if (!el.dataset.k) return;
  const hoy = hoyISO(), log = (await Store.get(el.dataset.k)) || { sesiones: [] };
  let s = log.sesiones.find(x => x.fecha === hoy);
  if (!s) { s = { fecha: hoy, series: [] }; log.sesiones.push(s); }
  const n = +el.dataset.n;
  s.series[n] = { ...(s.series[n] || {}), [el.dataset.f]: el.value === '' ? null : +el.value };
  log.sesiones = log.sesiones.slice(-30);
  Store.set(el.dataset.k, log);
});
```

Y en `routes`: `const routes = { rutina: renderRutina, dia: renderDia, ej: renderEjercicio };`

- [ ] **Step 3: Verificar en el browser**

Preview `gym`, mobile. Ir a Lun → primer bloque (Pect deck). Verificar: iframe de Drive carga y reproduce; cabecera "2+3 series · 10 - 8 - 6… · desc. 1'30""; badge APROX; 3 filas de inputs; "Sin registro previo". Cargar 20 kg × 10 en serie 1, salir del campo, recargar la página → el valor persiste. En consola: `localStorage.getItem('gym:pect-deck')` → JSON con `sesiones[0].fecha` de hoy. Cambiar la fecha guardada a ayer vía consola y recargar → "Última vez (…): 20×10" y placeholder `20` en kg. Bloque 6 (biserie) → dos artículos apilados, un solo botón de descanso. Último bloque → "Fin · volver al día".

- [ ] **Step 4: Commit**

```bash
git add store.js app.js && git commit -m "Pantalla Ejercicio: video, registro de kg/reps, última vez"
```

---

### Task 4: Timer de descanso

**Files:**
- Create: `timer.js`
- Modify: `app.js` (listener click para `[data-timer]`)

**Interfaces:**
- Consumes: `#timer` overlay de `index.html` (`<b>` para el número, `#timer-close`).
- Produces: `Timer.start(seg)`, `Timer.close()`.

- [ ] **Step 1: Escribir `timer.js`**

```js
const Timer = (() => {
  const el = document.getElementById('timer'), num = el.querySelector('b');
  let finAt = 0, tick = 0, ctx;
  const audio = () => { try { ctx ||= new (window.AudioContext || window.webkitAudioContext)(); ctx.resume?.(); } catch {} return ctx; };
  const beep = () => { const c = audio(); if (!c) return; const o = c.createOscillator(), g = c.createGain(); o.connect(g); g.connect(c.destination); o.frequency.value = 880; g.gain.setValueAtTime(.4, c.currentTime); g.gain.exponentialRampToValueAtTime(.001, c.currentTime + .8); o.start(); o.stop(c.currentTime + .8); };
  const paint = () => {
    const r = Math.max(0, Math.ceil((finAt - Date.now()) / 1000));
    num.textContent = `${Math.floor(r / 60)}:${String(r % 60).padStart(2, '0')}`;
    if (!r) { clearInterval(tick); tick = 0; el.classList.add('done'); beep(); }
  };
  // ponytail: sin aviso con la pantalla bloqueada (iOS pausa la página); al volver, visibilitychange repinta con la hora real.
  function start(seg) { audio(); finAt = Date.now() + seg * 1000; el.classList.remove('done'); el.hidden = false; clearInterval(tick); paint(); tick = setInterval(paint, 250); }
  function close() { clearInterval(tick); tick = 0; el.hidden = true; }
  document.addEventListener('visibilitychange', () => { if (!document.hidden && !el.hidden && tick) paint(); });
  document.getElementById('timer-close').addEventListener('click', close);
  return { start, close };
})();
```

- [ ] **Step 2: Enganchar en `app.js` (al final)**

```js
view.addEventListener('click', ev => { const b = ev.target.closest('[data-timer]'); if (b) Timer.start(+b.dataset.timer); });
```

- [ ] **Step 3: Verificar en el browser**

Ejercicio → tap "Descanso 1'30"" → overlay con `1:30` bajando; "Listo" cierra. En consola: `Timer.start(3)` → a los 3 s fondo rojo, beep, `0:00`; "Listo" cierra y vuelve a fondo normal.

- [ ] **Step 4: Commit**

```bash
git add timer.js app.js && git commit -m "Timer de descanso con beep"
```

---

### Task 5: Videos, Comida, Técnicas

**Files:**
- Modify: `app.js` (3 renders + rutas `videos`, `video`, `comida`, `tecnicas`)

**Interfaces:**
- Consumes: `DATA.videos`, `DATA.comida`, `DATA.tecnicas`, `video(id)`, `badge`, `esc`, `slug`.

- [ ] **Step 1: Agregar a `app.js` (antes de `const routes`)**

```js
function renderVideos() {
  const grupos = [...new Set(DATA.videos.map(v => v.grupo))];
  return `<header class="top"><span class="eyebrow">${DATA.videos.length} videos · @aguspazfit</span><h1>Videos</h1></header>` +
    grupos.map(g => `<h2>${esc(g)}</h2><div class="list">${DATA.videos.filter(v => v.grupo === g).map(v => `<a class="row" href="#/video/${v.id}"><b>${esc(v.titulo)}</b><span class="play">▶</span></a>`).join('')}</div>`).join('');
}
function renderVideo(id) {
  const v = DATA.videos.find(x => x.id === id); if (!v) return renderVideos();
  return `<header class="top"><a class="back" href="#/videos">‹ Videos</a><span class="eyebrow">${esc(v.grupo)}</span><h1>${esc(v.titulo)}</h1></header>${video(v.id)}`;
}
function renderComida() {
  const c = DATA.comida;
  return `<header class="top"><span class="eyebrow">${c.kcal} kcal · 3 comidas · pesos crudos</span><h1>Alimentación</h1></header>
  <div class="macros"><div><small>Proteína</small><b>~${c.macros.P}g</b></div><div><small>Grasas</small><b>~${c.macros.G}g</b></div><div><small>Carbos</small><b>~${c.macros.C}g</b></div></div>
  ${c.comidas.map(m => `<section class="meal m-${slug(m.nombre)}"><h2>${esc(m.nombre)}<small>${esc(m.kcal)}</small></h2>
    ${m.opciones.map((o, i) => `<details${i ? '' : ' open'}><summary><b>${esc(o.nombre)}</b><span>~${o.kcal} kcal</span><small>P ${o.P} g · G ${o.G} g · C ${o.C} g</small></summary><ul>${o.items.map(x => `<li>${esc(x)}</li>`).join('')}</ul></details>`).join('')}
    ${m.nota ? `<p class="nota">${esc(m.nota)}</p>` : ''}</section>`).join('')}
  <h2>Reglas</h2><dl class="reglas">${c.reglas.map(r => `<dt>${esc(r.titulo)}</dt><dd>${esc(r.texto)}</dd>`).join('')}</dl>`;
}
const renderTecnicas = () => `<header class="top"><span class="eyebrow">Referencia rápida</span><h1>Técnicas</h1></header><dl class="reglas">${DATA.tecnicas.map(t => `<dt>${badge(t.nombre)}</dt><dd>${esc(t.texto)}</dd>`).join('')}</dl>`;
```

`routes` final: `{ rutina: renderRutina, dia: renderDia, ej: renderEjercicio, videos: renderVideos, video: renderVideo, comida: renderComida, tecnicas: renderTecnicas }`.

- [ ] **Step 2: Verificar en el browser**

Videos: 8 grupos, 47 filas, tap → reproduce. Comida: 3 macros, 3 secciones con colores dorado/azul/violeta, primera opción abierta, reglas. Técnicas: 6 badges con color. Nav marca la pestaña correcta en cada una. Sin scroll horizontal.

- [ ] **Step 3: Commit**

```bash
git add app.js && git commit -m "Pantallas Videos, Comida y Técnicas"
```

---

### Task 6: PWA + deploy a GitHub Pages

**Files:**
- Create: `manifest.json`, `icon-512.png`, `apple-touch-icon.png`, `README.md`

- [ ] **Step 1: `manifest.json`**

```json
{ "name": "Entrenamiento", "short_name": "Gym", "start_url": "./", "scope": "./", "display": "standalone", "background_color": "#0c0b09", "theme_color": "#0c0b09", "icons": [{ "src": "icon-512.png", "sizes": "512x512", "type": "image/png" }] }
```

- [ ] **Step 2: Íconos con ffmpeg (drawtext)**

```bash
F="/System/Library/Fonts/Supplemental/Arial Bold.ttf"
ffmpeg -y -f lavfi -i color=c=0x0c0b09:s=512x512 -vf "drawtext=fontfile='$F':text='GYM':fontcolor=0xc9a84c:fontsize=200:x=(w-tw)/2:y=(h-th)/2-10" -frames:v 1 icon-512.png
sips -z 180 180 icon-512.png --out apple-touch-icon.png
```

Si `ffmpeg -filters | grep drawtext` no lista el filtro: escribir `icon.svg` (rect negro + `<text>` dorado "GYM", Arial bold 200px centrado) y `qlmanage -t -s 512 -o . icon.svg && mv icon.svg.png icon-512.png`.

- [ ] **Step 3: `README.md`**

```markdown
# Entrenamiento

PWA con la rutina de Agustín Paz (@aguspazfit). Abrir en Safari → Compartir → Agregar a pantalla de inicio.

- `data.js` — plan, videos, comida, técnicas. Para mes 2: editar y correr `node check_data.js`.
- `node test.js` — helpers.
- Preview local: `python3 -m http.server 8765`.
```

- [ ] **Step 4: Correr ambos checks**

Run: `node check_data.js && node test.js`
Expected: `OK · 4 días · 47 videos · 3 comidas · 6 técnicas` y `OK util`.

- [ ] **Step 5: Commit + repo + Pages**

```bash
git add -A && git commit -m "PWA: manifest, íconos, README"
gh repo create entrenamiento --public --source=. --push
gh api -X POST repos/Marquitomeschini/entrenamiento/pages -f 'source[branch]=main' -f 'source[path]=/'
```

Esperar el build: `gh api repos/Marquitomeschini/entrenamiento/pages --jq .status` hasta `built` (≈1 min). Abrir `https://marquitomeschini.github.io/entrenamiento/` en el browser pane con viewport mobile: Rutina carga, un video reproduce, `manifest.json` responde 200.

- [ ] **Step 6: Entregar**

Mensaje al usuario: URL, pasos "Safari → Compartir → Agregar a pantalla de inicio", límites (internet, timer con pantalla bloqueada, pesos solo en ese teléfono), y que el artifact de prueba 🧪 lo borre desde claude.ai.
